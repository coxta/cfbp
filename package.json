{
    "private": true,
    "type": "module",
    "scripts": {
        "local": "sudo rm -rf vendor.nosync && sudo rm -rf vendor && mkdir vendor.nosync && ln -s vendor.nosync vendor && composer install && sudo rm -rf node_modules.nosync && sudo rm -rf node_modules && mkdir node_modules.nosync && ln -s node_modules.nosync node_modules && npm install",
        "dev": "vite",
        "build": "vite build"
    },
    "devDependencies": {
        "@tailwindcss/forms": "^0.5.2",
        "@tailwindcss/typography": "^0.5.10",
        "alpinejs": "^3.4.2",
        "autoprefixer": "^10.4.2",
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.8.0",
        "postcss": "^8.4.6",
        "tailwindcss": "^3.1.0",
        "vite": "^4.0.0"
    },
    "dependencies": {
        "chart.js": "^4.4.0"
    }
}
